apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: erp-rabbitmq
  namespace: default
spec:
  serviceName: erp-rabbitmq
  replicas: 1
  selector:
    matchLabels:
      app: erp-rabbitmq
  template:
    metadata:
      labels:
        app: erp-rabbitmq
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3.13-management-alpine
          ports:
            - containerPort: 5672
              name: amqp
            - containerPort: 15672
              name: management
          env:
            - name: RABBITMQ_DEFAULT_USER
              value: erp_user
            - name: RABBITMQ_DEFAULT_PASS
              valueFrom:
                secretKeyRef:
                  name: erp-rabbitmq-secret
                  key: password
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: erp-rabbitmq
  namespace: default
spec:
  selector:
    app: erp-rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672